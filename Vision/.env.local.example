# ==============================================================================
# Local Configuration Template
# Copy this file to '.env.local' and update with your values
# ==============================================================================

# API Path Configuration (Client-Side)
# ------------------------------------------------------------------------------
# To avoid `CORS` issues during local development, use the Next.js server (/dev) as a proxy.
# Browser request flow: localhost:3000/dev/... -> CloudFront/...
NEXT_PUBLIC_LIVE_API_URL="/dev"

# Static Data Handling (Client-Side)
# ------------------------------------------------------------------------------
# NEXT_PUBLIC_USE_REMOTE_STATIC_DATA:
# - 'false' (recommended): Use local files in the 'public/data' folder for faster development
# - 'true': Use when testing remote data (requires STATIC_API_URL="/dev" setting)
NEXT_PUBLIC_USE_REMOTE_STATIC_DATA="false"

# Data source path:
# - For local testing: "/data" (access public/data folder)
# - For remote testing: "/dev" (access CloudFront data via proxy)
NEXT_PUBLIC_STATIC_API_URL="/data"

# Proxy Destination (Development Only)
# ------------------------------------------------------------------------------
# Important: This variable is only used in the 'next.config.ts' rewrites configuration.
# It is not exposed to the browser (no NEXT_PUBLIC prefix) and represents the actual remote address the proxy will reach.
REMOTE_API_URL="https://your-distribution-id.cloudfront.net"

# Local Map Style (Optional)
# ------------------------------------------------------------------------------
# If you are editing the map style locally, you can set it as follows:
# NEXT_PUBLIC_MAP_URL="/map/style.json"
