# ==============================================================================
# Production Configuration Template
# Refer this file and update with your values at deployment
# ==============================================================================

# API Configuration
# ------------------------------------------------------------------------------
# The url to get live bus information and other data.
# Set `YOUR_CLOUDFRONT_DISTRIBUTION` to your actual CloudFront distribution domain.
NEXT_PUBLIC_LIVE_API_URL="https://YOUR_CLOUDFRONT_DISTRIBUTION.cloudfront.net"

# Static Data Configuration
# ------------------------------------------------------------------------------
# The S3 bucket or CloudFront URL where static JSON data (route info, etc.) is stored.
# Set `YOUR_S3_BUCKET` to your actual S3 bucket name.
NEXT_PUBLIC_STATIC_API_URL="https://YOUR_S3_BUCKET.s3.amazonaws.com"

# 'true': Fetch data from remote URL (STATIC_API_URL)
# 'false': Use local data from `public/data`
NEXT_PUBLIC_USE_REMOTE_STATIC_DATA="true"

# Map Settings (Optional)
# ------------------------------------------------------------------------------
# Custom map style sheet URL (if not set, the default is used)
# NEXT_PUBLIC_MAP_URL="https://tiles.openfreemap.org/styles/liberty"
